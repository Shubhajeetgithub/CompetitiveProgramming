#include<bits/stdc++.h>
using namespace std;
// Debug macro: only prlong longs in LOCAL mode
#ifdef LOCAL
#define debug(x) cerr << #x << " = " << x << endl
#else
#define debug(x)
#endif
using ll = long long;
ll V = 1000000000LL;
void solve() {
    int n;
    cin >> n;
    
    cout << flush;

}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t = 1;
    cin >> t;
    debug(t);
    while (t--) {
        solve();
    }
    return 0;
}